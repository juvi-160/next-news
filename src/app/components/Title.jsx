import React from 'react';

const Title = ({ title }) => {
  return (
    <div className='pl-10'>
      <h1 className="text-2xl font-bold ">{title}</h1>
    </div>
  );
};

export default Title;
